export interface Zone {
    id: number;
    codigoZona: string;
    promotor: string;
    supervisor: string;
}

export interface Promoter {
  promotor: string;
}

export interface Supervisor {
  supervisor: string;
}

  
