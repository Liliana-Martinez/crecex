export interface Zone {
    id: number;
    codigoZona: string;
    promotora: string;
    supervision: string;
  }

  export interface Promoter {
  promotor: string;
}

export interface Supervisor {
  supervisor: string;
}
  
